# URL of the Cloudflare Worker that proxies The Odds API.
# Deploy the worker from cloudflare/odds-proxy/ (see its README) and paste the
# resulting *.workers.dev URL here. When unset, the scoreboard hides the
# "Refresh Odds" button and renders normally.
VITE_ODDS_PROXY_URL=
